##Angular What do You know?

- one way data binding in a component ( interpolatioin )
- two way data binding  in a component
- iteration over an array from data in the same folder
- using a template for building/displaying data
- using logic with directives: *ngFor *ngIf
- using pipes
- using custom pipes
- passing data from parent component to child component
- passing data from child component to parent component
- using js functions like: filter, IndexOf, Splice
- nesting components: app -> product-list -> product-detail
- building a service
- registering the service
- inject the service into dependency 
- retrieving data using HTTP 
- sending data using HTTP
- routing
- define routes
- using parameters in the route
- writing a guard for protecting routes
- separating project up in different modules


